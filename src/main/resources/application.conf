usersDB = {
  driver = "org.h2.Driver"
  url = "jdbc:h2:testdb-users"
  numThreads = 5
}

shippedOrdersDB = {
  driver = "org.h2.Driver"
  url = "jdbc:h2:testdb-shippedOrders"
  numThreads = 5
}

openOrdersDB = {
  driver = "org.h2.Driver"
  url = "jdbc:h2:testdb-openOrders"
  numThreads = 5
}

reportingDB = {
  driver = "org.h2.Driver"
  url = "jdbc:h2:testdb-reporting"
  numThreads = 5
}
